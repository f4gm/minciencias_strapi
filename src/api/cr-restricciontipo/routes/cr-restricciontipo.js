'use strict';

/**
 * cr-restricciontipo router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::cr-restricciontipo.cr-restricciontipo');
